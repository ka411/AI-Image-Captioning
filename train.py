from model import create_caption_model

# Placeholder training script (requires dataset preprocessing)
vocab_size = 5000
model = create_caption_model(vocab_size=vocab_size)

# Example:
# model.fit([train_features, train_sequences], epochs=20)
# model.save("image_caption_model.h5")
